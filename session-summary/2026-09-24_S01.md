# Session handoff — 2026-09-24, S01

## 0. Session Metadata
- **Model:** Opus 5.5 (`claude-opus-5-5`), from the session configuration; no mid-session switch known.
- **Effort:** high (`$CLAUDE_EFFORT`, checked)
- **Date / session #:** 2026-09-24, S01 (first session; repo was an empty "Initial commit")

## 1. Goal
- Project: a static web app of Physics 20 / Physics 30 (Alberta) simulations for tutoring, whose readouts match hand calculations with the data sheet.
- This session: bring over the general workflow from `PeterFriedrich/cc-data-project-template` (minus the data-specific parts), and build phase 1: one sim per course unit.

## 2. Current State
### ✅ Completed
- Workflow from the template: `CLAUDE.md`, `TODO.md`, `CONTRIBUTING.md`, `docs/DECISIONS.md` (8 rows), `docs/SPEC_phase1.md`, `docs/ARCHITECTURE.md`, `/handoff` and `/project-audit` skills, SessionStart/End `handoff_gap.py` hooks, `.githooks/pre-push` (now `main`), `tools/todo_archive.py`, `tools/retrieval_report.py`.
  - Dropped as data-specific: `data/`, `DATA.md`, `DATA_ISSUES.md`, `requirements*.txt`, venv setup in `bootstrap.sh`.
  - Adapted: `check_decisions_log.py` recognises JS test IDs (`test("test_x", …)` in `tests/*.test.js`) and `tools/verify-*.js`; CUTOFF 2026-09-24. `check_doc_citations.py` scans `site/` and `tests/`. `test_loaded_path.py` ported to `tests/loaded-path.test.js`.
- Ten sims in `site/js/sims/`: projectile, incline, circular, spring, superposition (P20); collisions, magnetic, refraction, photoelectric, halflife (P30). Physics in `site/js/physics/`, 51 `node:test` tests passing.
- `tools/verify-sims.js`: every page passes in headless Chromium at 390 px and 1280 px, light and dark. A scripted interaction pass (TIR, cutting the string, carts never meeting / sticking, photoelectric threshold and stopping voltage, electron in B) matched hand calculations.
- CI: `.github/workflows/tests.yml` (merge gate) and `deploy.yml` (GitHub Pages).
### ⚠️ In Progress
- Nothing half-done. The PR for `claude/laughing-dirac-yajyhq` is the next step.
### ❌ Blocking Issues
- None. `deploy.yml` will fail until Pages is enabled (Settings → Pages → Source: GitHub Actions). That doesn't affect the merge gate.

## 3. Technical Learnings
- ES modules do not load from `file://`: use `npm run serve` (`tools/serve.js`, zero-dependency).
- Unicode has no subscript *g*: `₉` is subscript nine. Canvas labels use `subText()` in `lib/canvas.js` with `F_g` syntax.
- `[hidden]` needed `display: none !important`, because `.transport { display: flex }` overrode the attribute.
- Playwright `text=` selectors also match the "Try this" prompts. Scope to `#sim-controls` / `#sim-transport`.
- Floating-point residue (x ≈ 1e-17 at equilibrium) is snapped by `snap(x, scale)` relative to the quantity's own scale. No absolute threshold, because charges and masses are legitimately 10⁻¹⁹ and 10⁻³¹.
- The global Playwright lives at `/opt/node22/lib/node_modules/playwright`; `verify-sims.js` falls back to that path.

## 4. Next Steps (prioritized)
1. Owner: enable GitHub Pages, merge the PR, check the live site.
2. Owner walkthrough against hand calculations (TODO.md, first item).
3. Map sims to program-of-studies outcome codes. Not done yet: units are matched by title only, and codes shouldn't be shown until they're verified.
4. Next sims from `TODO.md` "More simulations". Propose each one first.

## 5. Restoration Procedure
```bash
./bootstrap.sh          # hooks path + tests + guards
npm run serve           # http://localhost:8000
npm run verify          # browser smoke test → output/*.png
```

---

# Addendum — same session, phase 2 (Physics 30 depth)

## 0. Session Metadata
- Same model and effort as above. PR #1 merged and Pages enabled by the owner; the deploy re-run succeeded. From this sandbox I can't reach `github.io`, so I didn't load the live site myself.

## 1. Goal
- The owner asked to focus on Physics 30 and picked: Coulomb's law / electric fields (incl. parallel plates) and 2-D collisions. Physics 20 stays first on the home page.

## 2. Current State
### ✅ Completed
- `docs/SPEC_phase2.md`. New physics: `electrostatics.js` (Coulomb, field sum, net force, plates trajectory with work-energy), `momentum2d.js` (smooth-disc glancing collision, stick case), `vectors.js`. 65 tests.
- New sims: `coulomb` (drag charges and probe P), `plates` (electron/proton/alpha, polarity, hits-plate case), `collisions2d` (aim offset, tip-to-tail momentum diagram). Catalog entries added.
- `verify-sims.js` passes at 390/1280. Hand checks on screen: F = 0.0843 N; plates deflection 0.549 cm and 13.7 eV; equal-mass elastic glancing collision at 90°; −15 cm aim gives 1.50 m/s at 41.4° below and 1.32 m/s at 48.6° above.
- DECISIONS row for phase 2 and the disc model; ARCHITECTURE §7 lists the new teaching models.
### ⚠️ In Progress
- None. Next is the PR for phase 2.

## 3. Technical Learnings
- e = 0 in 2-D is not "stick together": the discs would still slide apart tangentially. Physics 30's "perfectly inelastic" is the separate `stick` case.

## 4. Next Steps
1. Owner walkthrough (TODO.md).
2. Remaining Physics 30 candidates in TODO.md: velocity selector / mass spectrometer, induction, potential, double slit, Bohr model.

---

# Addendum 2 — cache busting on deploy

- The owner didn't see PR #2's sims after a successful deploy. Cause: Pages' `max-age=600` let a fresh page run against a cached old `catalog.js`.
- Fix: `tools/build-site.js`, ported from edmonton-tax-viz's `scripts/build_site.py` `?v=<content hash>` pattern and extended with an import map so nested and dynamic ES-module imports are versioned too. `deploy.yml` now publishes the built `_site/`.
- Guards: `tests/build-site.test.js`; `npm run verify:built` fails on any unversioned `.js`/`.css` request. I mutation-tested it: dropping `catalog.js` from the map failed every sim page.
- Remaining limit: HTML itself can be up to 10 min stale; a hard refresh fixes that.

---

# Final wrap-up — end of S01

## 0. Session Metadata
- **Model:** Opus 5.5 (`claude-opus-5-5`) per the session configuration throughout; no `/model` switch seen.
- **Effort:** `high` when phase 1 was built (checked then). `medium` now (`$CLAUDE_EFFORT`, checked at wrap-up), so phase 2 and the cache-busting work may have run at the lower level. Give those a closer review pass.
- **Date / session #:** 2026-09-24, S01.

## 1. Goal
- Project: a static Physics 20/30 (Alberta) tutoring site whose readouts match hand calculations with the data sheet.
- This session: workflow ported from cc-data-project-template; phase 1 (10 sims); phase 2 (3 Physics 30 sims); deploy + cache busting.

## 2. Current State
### ✅ Completed (all merged to `main`, deployed)
- PR #1: app + workflow + 10 sims. PR #2: `coulomb`, `plates`, `collisions2d`. PR #3: `tools/build-site.js` cache busting.
- GitHub Pages is enabled; deploy runs #1 (re-run), #2 and #3 all succeeded. Live at https://peterfriedrich.github.io/physics_sim/. I haven't checked it from this sandbox, because the proxy blocks `github.io`.
- 69 tests; `npm run check`, `npm run verify` and `npm run verify:built` all pass at 390/1280 px.
### ⚠️ In Progress
- Nothing. The working tree is clean apart from this handoff.
### ❌ Blocking Issues
- None.

## 3. Technical Learnings
- **Pages caching:** `max-age=600` on every file. Fixed for JS/CSS by content-hash URLs plus an import map (`tools/build-site.js`). HTML can still be ≤10 min stale; tell the owner to hard refresh.
- **Deploy publishes `_site/`, not `site/`.** Any new top-level HTML page must keep the `<link rel="stylesheet">` + `<script type="module" src>` shape, or `build-site.js` fails loudly (by design).
- **Merged-PR branches:** after each merge, restart `claude/laughing-dirac-yajyhq` from `origin/main` (`git checkout -B … origin/main`, then `--force-with-lease` push). The pre-push hook only warns via `gh`, which isn't available in this VM.
- Owner preferences seen: Physics 30 first for new sims, but Physics 20 stays first on the home page. They reuse patterns from their other repos (cc-data-project-template, edmonton-tax-viz), so check those before inventing.
- `sleep` is blocked in the foreground; wait on a CI/deploy with `send_later`.

## 4. Next Steps (prioritized)
1. Owner: hard refresh the live site and confirm all 13 sims appear (10 phase 1 + 3 phase 2).
2. Owner walkthrough of every sim against hand calculations (first item in TODO.md).
3. Update TODO.md's "Enable GitHub Pages" item: it's done (the deploy succeeded). Archive it with `python3 tools/todo_archive.py` after ticking it, and add the site URL to README.md.
4. Next Physics 30 sims (TODO.md): velocity selector / mass spectrometer, EM induction, electric potential, double slit, Bohr model. Propose each one first.
5. Outcome-code mapping, verified against the Alberta programs of study (TODO.md).

## 5. Restoration Procedure
```bash
./bootstrap.sh            # hooks + tests + guards (no installs)
npm run serve             # http://localhost:8000 (unbuilt site/)
npm run verify            # browser smoke test → output/*.png
npm run verify:built      # the deploy build, checks every asset is versioned
```

---

# Addendum 3 — phase 3 (Physics 20 depth), recorded after the fact

## 0. Session Metadata
- **Model:** Opus 5.5 (`claude-opus-5-5`) per session configuration.
- **Effort:** `medium` (`$CLAUDE_EFFORT`, checked when this was written).
- ⚠️ **Written from the commits, not from memory.** Phase 3 was built in this same session, but that stretch of conversation is no longer in my context. The SessionStart hook flagged 5 commits with no handoff. Everything below comes from `git log`, the diff, the docs and a re-run of every check. The design reasoning lives in `docs/SPEC_phase3.md` and its DECISIONS row, not here. Review phase 3 more closely than the earlier work.

## 1. Goal
- Fill in Physics 20 beyond one sim per unit. The owner approved a 9-sim plan (SPEC_phase3.md, "Proposed and approved 2026-09-24").

## 2. Current State
### ✅ Completed (merged to `main` via PR #6; deploy run #6 succeeded 06:54 UTC)
- PR #4 (handoff) and PR #5 (Pages TODO closed; README links the live site) are merged too.
- Physics modules, each tested against a worked example: `motion1d.js`, `relative.js`, `gravitation.js`, `connected.js`, `track.js`, plus additions to `oscillations.js` (`pendulumState`) and `waves.js` (air columns, `nodesAndAntinodes`, Doppler).
- Sims: `motiongraphs`, `river` (20 A); `gravitation`, `atwood` (20 B); `coaster`, `orbits` (20 C); `pendulum`, `aircolumn`, `doppler` (20 D).
- The site now has 22 sims: 14 Physics 20 and 8 Physics 30. 94 tests.
- Commit `9dbe435` fixed a bug: `distanceTravelled` was leaking x₀ into the path length. A test was added.
- DECISIONS row (phase 3): motion graphs are slider-driven so readouts stay exact. The coaster car is the one stepped motion, and its readouts come from the energy equation, not the step (ARCHITECTURE §4/§7).
- Re-verified now, on `main` at `96556e9`:
  - `node --test`: 94 pass.
  - Both doc guards: OK.
  - `verify-sims.js` at 390 and 1280 px: all 23 pages (22 sims + home) render without errors.
  - The built-site check (`npm run verify:built`) was still running when this was written; its result goes in the handoff PR description.
### ⚠️ In Progress
- None.
### ❌ Blocking Issues
- None.

## 3. Technical Learnings
- If the conversation context is lost mid-session, the handoff hook is what surfaces the gap. This addendum exists because of it, so keep writing handoffs per phase, not only at the end.
- A leftover check-in for PR #6 (`trig_01MsefBD2BxnbSuef1AMhp5J`) was still armed after the merge; I deleted it. Clear triggers when a PR merges.

## 4. Next Steps (prioritized)
1. Owner: hard refresh the live site and confirm all 22 sims appear.
2. Owner walkthrough against hand calculations (TODO.md), especially the phase 3 sims, which were never hand-checked in a handoff.
3. Remaining Physics 30 candidates (TODO.md): velocity selector / mass spectrometer, induction, electric potential; double slit, EM spectrum; Bohr model, mass defect. Propose each one first.
4. Outcome-code mapping (TODO.md).

## 5. Restoration Procedure
- Unchanged from the wrap-up above.
