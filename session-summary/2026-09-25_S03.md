# Session S03 — 2026-09-24/25 (merge confirmation after phase 4)

## 0. Session Metadata
- **Model:** Opus 5.5 (`claude-opus-5-5`), per session configuration; no `/model` switch seen. This is the same continued conversation that built phase 4 (see the addendum at the end of `2026-09-24_S01.md`); context was compacted once.
- **Effort:** `medium` (`$CLAUDE_EFFORT`, checked).
- **Date / session #:** 2026-09-25, S03.

## 1. Goal
- Project: a static web app of Physics 20 and Physics 30 (Alberta) simulations for tutoring. Every readout must match a hand calculation done with the course data sheet.
- This session: see phase 4 (PR 11) through to merge and deploy. No new features.

## 2. Current State
### ✅ Completed
- The owner merged `PeterFriedrich/physics_sim#11` (phase 4, 7 Physics 30 sims) at 2026-09-24 16:17 UTC. The merge commit on `main` is `7c2f0e7`.
- `git merge-base --is-ancestor` confirmed that the branch's commits (`af4af1c`, `4e4e284`, `338c044`) are on `origin/main`.
- The "Deploy site" run (Actions run 36026292751) succeeded. The live site has 29 sims: https://peterfriedrich.github.io/physics_sim/
- The branch `claude/laughing-dirac-yajyhq` was restarted from `origin/main`. It carries only this handoff.
### ⚠️ In Progress
- None. No open PR other than the one carrying this handoff.
### ❌ Blocking Issues
- None.

## 3. Technical Learnings
- Nothing new this session. The phase 4 learnings are in the S01 file's addendum:
  - Physics 20 uses Earth mass 5.98 × 10²⁴ kg and Physics 30 uses 5.97 × 10²⁴ kg. Check `docs/DATA_SHEET.md` per course.
  - Playwright counts hidden controls in `nth()`.
- Owner rule, repeated because it costs usage: subscribe to PR activity, but never arm a `send_later` or trigger check-in (CLAUDE.md).

## 4. Next Steps (prioritized)
1. At session start, check open GitHub issues (CLAUDE.md), then read `TODO.md` → `## Open work`.
2. Owner walkthrough of every sim against hand calculations (TODO.md, first open item). This is owner-driven; offer the worked numbers listed in the S01 addendum.
3. Only if the owner asks for it (propose the plan first, since it's a new sim), pick up the remaining sim candidates:
   - Qualitative induction (magnet and coil, no numeric emf).
   - Photon momentum and Compton scattering.
   - Quarks and the fermion table.
4. Outcome-code mapping against the Alberta programs of study (TODO.md).

## 5. Restoration Procedure
- `./bootstrap.sh` (sets `core.hooksPath .githooks`), then `npm run check`, `npm run serve` and `npm run verify`.
- Remote VM quirks: `docs/REMOTE_VM.md`. Playwright lives at `/opt/node22/lib/node_modules/playwright`.
- After any merge: `git fetch origin main && git checkout -B claude/laughing-dirac-yajyhq origin/main`, then push with `--force-with-lease`.
